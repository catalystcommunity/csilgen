// Stable coordinates so a consumer can `includeBuild(...)` this project and depend on
// "community.catalyst.csilgen:csilgen-transport" without a dependencySubstitution block.
rootProject.name = "csilgen-transport"
