StandaloneType = {
    @bidirectional
    value: text,
    @bidirectional
    metadata: {
        created: int,
        version: text
    }
}

service StandaloneService {
    process: StandaloneType -> { success: bool }
}